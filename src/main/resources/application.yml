telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN}
    username: ${TELEGRAM_BOT_USERNAME}

openai:
  api-key: ${OPENAI_API_KEY}
  model: gpt-4.1-mini
  base-url: https://api.openai.com/v1
  max-tokens: 300

google:
  drive:
    credentials-path: ${GOOGLE_CREDENTIALS_PATH:credentials/google-service-account.json}
    root-folder-name: sales
